Sub2Ops 0.1.4 修复瞬时响应报错和流式文本空格丢失，简化连接测试，移除 Telegram，并让 Dock 图标随主窗口开关。

- 网络错误区分网关异常、读取中断、超时、空响应、鉴权和数据结构问题；保留最后有效快照，恢复连接后继续刷新，操作不自动重放。
- 测试弹窗点击开始直接执行，保留模式、模型和素材选择；返回文本保留空格、换行及缩进。额度批次完成后停止进度轮询。
- OAuth 设置独立保存，保留现场每日测活、自动恢复、Bark、两平台 Key 回退及账号配置；网页自动化入口调整为 /ops。云端需先执行本版配置迁移并部署配套接口。
- 主窗口打开或最小化时显示 Dock 图标，关闭后隐藏；菜单栏快捷面板继续驻留，保留 macOS 27 左键和右键修复。

系统要求：macOS 12+，Apple Silicon。使用已有管理员 API Key 与 macOS Keychain。本版为本机签名，未经 Apple Developer ID 签名或公证。检查更新打开发布页，不自动安装。

安装前使用 shasum -a 256 -c SHA256SUMS 校验下载文件。
