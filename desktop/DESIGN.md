# Sub2Ops 桌面设计

这是持续运行的运维控制台。优先看清最近成功调用、当前调度和错误证据，再执行准确的单账号操作。

- 采用 Linear 参考中的紧凑层级和细分隔线，适配 macOS 系统字体及深浅主题；不使用其专有字体或品牌素材。
- 浅色：画布 #f5f6f9、表面 #ffffff、正文 #202432、次要文字 #707789、强调 #6262ce、边线 #e5e7ed。深色使用 #17191f、#20232b、#ebedf3、#969eaf、#9797ef、#333844。
- 字号 12 / 13 / 14 / 18 / 26；数字等宽，代码才使用等宽字体。正文左对齐，数值右对齐。
- 主窗口 1180×800：192px 侧栏 + 工作区，错误详情为右侧抽屉。快捷面板 440×640：连接状态与刷新、固定、关闭；分组 / 异常切换；滚动内容与更新时间底栏。
- 每组展示最近成功调用的三个不同账号；收藏优先，账号行显示模型、准确调用时间与调度开关。
- 时间统一为北京时间 `YYYY-MM-DD HH:mm:ss`。没有记录显示“暂无记录”，无效时间显示“时间未知”；不使用相对时间。
- 账号“用量窗口”对齐 Sub2API：OpenAI OAuth 的 5h/7d；Grok 的上游请求数与 Token 限额窗口。显示已用百分比、采集和重置时间，历史或异常数据标记状态；未知不显示为 0%。不以账单或金额预算代替用量窗口。
- 删除解释性副标题、连接教学、实现原理和装饰口号。保留真实状态、错误原因及托管解除等必要确认。
- 账号与分组使用列表、表格；不堆叠无意义的数字卡片。状态始终有文字和图标，不只靠颜色。
- 蓝紫色用于选择与操作；红色用于错误，琥珀色用于冷却，绿色用于正常。调度开关和可用性是独立字段。
- 所有操作具备加载、失败、禁用和键盘焦点状态。确认托管解除后才写入。离线不允许提交或重放操作。
- 菜单栏左键始终显示并聚焦面板，重复点击不隐藏；真正失焦时关闭，固定状态除外。Escape 和关闭按钮均可关闭。主窗口关闭驻留菜单栏，Dock 始终不显示图标。减少动态效果，尊重 reduced-motion。
- 验收包括原生窗口与 Browser：深浅主题、窄面板、键盘、长账号名、错误正文、滚动、空与离线状态。
